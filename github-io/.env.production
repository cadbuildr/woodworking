# Production build (GitHub Pages): the PUBLISHABLE keyId half of the partner
# key (cbsdk_<keyId>_<secret>) — never put the full key (the secret half)
# here. The SDK mints short-lived cbv1 tokens from the hub, which enforces
# the key's allowed_origins against the browser Origin.
VITE_CADBUILDR_KEY_ID=7b284df9e9787db1
